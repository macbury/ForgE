game.module("$log", function () {
  return {
    info: function(tag, text) {
      Gdx.app.log(tag, text);
    },
    debug: function(tag, text) {
      Gdx.app.debug(tag, text);
    }
  };
});

game.run(function ($log) {
  $log.info("main.js", "Initialized game...");
});
